import type { Configure } from '@easy-editor/core'
import MPieChart from './component'

const configure: Configure = {
  props: [],
  component: {},
  supports: {},
  advanced: {
    view: MPieChart,
  },
}

export default configure
